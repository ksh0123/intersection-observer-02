const DetectArea = ({ detectAreaRef }) => {
  return (
    <div
      ref={detectAreaRef}
      className="bg-white text-white w-full py-12 flex justify-center items-center text-xs"
    >
      DetectArea
    </div>
  );
};

export default DetectArea;
